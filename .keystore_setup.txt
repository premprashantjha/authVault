KEYSTORE SETUP COMPLETE ✓
=========================

Keystore Location: android/app/release.keystore
Alias: release
Valid Until: April 7, 2053

IMPORTANT: Set these environment variables before building release APK
-------------------------------------------------------------------------

For PowerShell (current session):
---------------------------------
$env:AUTHENTICATOR_KEYSTORE_FILE="android/app/release.keystore"
$env:AUTHENTICATOR_KEYSTORE_PASSWORD="<YOUR_KEYSTORE_PASSWORD>"
$env:AUTHENTICATOR_KEY_ALIAS="release"
$env:AUTHENTICATOR_KEY_PASSWORD="<YOUR_KEY_PASSWORD>"

For Permanent Setup (add to PowerShell profile):
------------------------------------------------
[System.Environment]::SetEnvironmentVariable('AUTHENTICATOR_KEYSTORE_FILE', 'android/app/release.keystore', 'User')
[System.Environment]::SetEnvironmentVariable('AUTHENTICATOR_KEYSTORE_PASSWORD', '<YOUR_KEYSTORE_PASSWORD>', 'User')
[System.Environment]::SetEnvironmentVariable('AUTHENTICATOR_KEY_ALIAS', 'release', 'User')
[System.Environment]::SetEnvironmentVariable('AUTHENTICATOR_KEY_PASSWORD', '<YOUR_KEY_PASSWORD>', 'User')

BUILD COMMANDS
--------------
# Build release APK
flutter build apk --release

# Build release App Bundle (for Play Store)
flutter build appbundle --release

# Install release APK to device
flutter install --release

SECURITY REMINDERS
------------------
1. ✓ Keystore created with strong 2048-bit RSA key
2. ✓ Valid for 10,000 days (27+ years)
3. ⚠️  BACKUP THIS FILE - Store in secure location with password manager
4. ⚠️  NEVER commit release.keystore to Git (already in .gitignore)
5. ⚠️  Store passwords in secure password manager, not in code
6. ✓ ProGuard will obfuscate and strip debug logs in release builds
7. ✓ Screenshot prevention active in production
8. ✓ Root detection will warn users on compromised devices

BACKUP INSTRUCTIONS
-------------------
1. Copy android/app/release.keystore to secure backup location
2. Encrypt with GPG: gpg -c release.keystore
3. Store encrypted file in password manager or secure cloud storage
4. Document the passwords securely

Certificate Details:
-------------------
Owner: CN=prem jha, OU=Embedded, O=cdac, L=noida, ST=UP, C=91
SHA256: A5:B5:5E:8C:35:21:44:FC:CF:99:0E:26:49:FF:54:B8:BD:5D:E8:0A:99:3E:FB:96:C1:AA:91:8E:0F:B8:58:96
